import { SerachByNamePipe } from './serach-by-name.pipe';

describe('SerachByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SerachByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
