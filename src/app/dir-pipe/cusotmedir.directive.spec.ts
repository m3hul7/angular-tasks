import { CusotmedirDirective } from './cusotmedir.directive';

describe('CusotmedirDirective', () => {
  it('should create an instance', () => {
    const directive = new CusotmedirDirective();
    expect(directive).toBeTruthy();
  });
});
